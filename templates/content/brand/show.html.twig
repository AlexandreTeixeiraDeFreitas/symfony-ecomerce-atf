{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="w3-button w3-teal" href="{{ path('app_brand_index') }}">back to list</a>

    <a class="w3-button w3-teal" href="{{ path('app_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('content/brand/_delete_form.html.twig') }}
{% endblock %}
