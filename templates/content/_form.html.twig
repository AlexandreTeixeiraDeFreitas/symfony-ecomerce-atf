{{ form_start(form, { 'attr': {'class': 'w3-container taille-max'}}) }}
    {{ form_widget(form) }}
    <button class="btn w3-btn w3-blue-grey">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
