<form method="post" action="{{ path('app_cartproducts_delete', {'id': cartproduct.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cartproduct.id) }}">
    <button class="btn">Delete</button>
</form>
